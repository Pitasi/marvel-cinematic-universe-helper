# marvel-cinematic-universe-helper
Little web application to track latest movies from Marvel Studios.

## Requirements
    $ sudo npm install -g bower
    $ sudo npm install -g grunt
    $ npm install
    $ bower install

## Start the application
    $ grunt && grunt serve
